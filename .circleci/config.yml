version: 2.1

orbs:
  win: circleci/windows@2.2.0

# Default actions to perform on each Emacs version
commands:
  setup-windows:
    steps:
      - checkout
      - run:
          name: Install Eldev
          command: |
            # Remove expired DST Root CA X3 certificate. Workaround
            # for https://debbugs.gnu.org/cgi/bugreport.cgi?bug=51038
            # bug on Emacs 27.2.
            gci cert:\LocalMachine\Root\DAC9024F54D8F6DF94935FB1732638CA6AD77C13
            gci cert:\LocalMachine\Root\DAC9024F54D8F6DF94935FB1732638CA6AD77C13 | Remove-Item
            (iwr https://raw.githubusercontent.com/ikappaki/eldev/issue/circle-failure-win/webinstall/circle-eldev.ps1).Content | powershell -command -
            eldev version
  test:
    steps:
      - run:
          name: Run regression tests
          command: eldev -dtT -p test

jobs:
  test-windows-emacs-latest:
    executor: win/default
    steps:
      - run:
          name: Install Emacs latest
          command: |
            choco install emacs
      - setup-windows
      - test

workflows:
  version: 2
  ci-test-matrix:
    jobs:
      - test-windows-emacs-latest
